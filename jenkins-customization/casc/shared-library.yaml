unclassified:
  globalLibraries:
    libraries:
      - name: "groom-shared-lib"
        defaultVersion: "main"
        implicit: true
        allowVersionOverride: true
        includeInChangesets: false
        retriever:
          modernSCM:
            scm:
              git:
                remote: "https://github.com/groomcloudteam2/jenkins-shared-lib.git"
                credentialsId: "github-token"
